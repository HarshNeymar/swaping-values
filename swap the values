#include<stdio.h>
int main()
{
int a,b,temp;

printf("Enter the value of a: );
scanf("%d",&a);

printf("Enter the value of b: );
scanf("%d",&b);

a=temp;
b=a;
temp=b;

printf("The values of a=%d and b=%d are swaped",a,b);

return 0;
}
